{
  "name": "clawnad-indexer",
  "version": "1.0.0",
  "description": "ClawNad subgraph indexer for Monad",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy clawnad-indexer",
    "create-local": "graph create --node http://localhost:8020/ clawnad-indexer",
    "remove-local": "graph remove --node http://localhost:8020/ clawnad-indexer",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 clawnad-indexer"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.98.1",
    "@graphprotocol/graph-ts": "^0.38.2"
  }
}
